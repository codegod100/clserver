{
  "name": "parenscript-cloudflare-worker",
  "version": "1.0.0",
  "description": "A dynamic Cloudflare Worker with routing generated using Parenscript from Common Lisp",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "sbcl --script build.lisp"
  },
  "keywords": ["parenscript", "cloudflare-worker", "common-lisp", "code-generation", "routing", "dynamic"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
